#### IDEAS
    TIP: toolkit components / layouts for huge number of items - must be same size probably
         row/column virtualization "scrolling" for same sized items
    TIP: add timestamp for event and eventual expiration
    TIP: optimize events and listeners - now event has to go trough all components and all their listeners
    TIP: recycling events: event objects can be reused
    TIP: relative scroll control or scroll by components - only applicable to some layouts ...
    TIP: add gravity to stacks
    TIP: add create methods instead of constructors
    TIP: raised / lowered borders
    TIP: automatic font size calculation to fit required size
    TIP: settings/parameter/attribute/style fetching for components - global vs class local vs object local vs group local settings
    add possibility for any component to be the root component
        find all usages of getWindow() to prevent errors and null pointers or cast errors

    optimalizace relayoutu - u size control nastavit dva booleany
        jeden říkající, zdali okolí komponenty ovlivní její velikost
        druhý říkající, zdali vnitřek komponenty ovlivní její velikost
        poté při relayoutu doskákat vždy jen do potřebného bodu
        možná by bylo třeba upřesnit, jakého rozměru by se měl relayout týkat... a mohlo by být složitější
            might take into account that component or content width might depend on height or similar
        anebo naopak... zdali okolí/obsah určitě neovlivní její velikost!

        změřit efektivitu
            počet navštívených komponent při jednom projetí
            počet provedených relayoutů při vybrané akci

    renderer - gets colros, fonts and other properties from a components and takes care of the drawing
        swing renderer
        opengl renderer
        other user defined renderers

#### LATER:
    add shape class - for mouse click or move-over detection - rectangle or oval as default
    designer fine tunning
        add something like a designer id property - might be better than 
        how about: check box and radio button size, scroll buttons size, etc.

    add insets getters to window too, not only impl window, might not need impl window getter then... MIGHT!
    horizontal / vertical slider
    cobmo box
    spinner
        extends or uses text field
        int / long
        float / double

#### SOON
    multiline text input
    multiline interactive text content
    zalamování textu v multiline text contentu
    

#### NOW
    fix test paddings and spacings
        přidat možnost přidávání značek ke komponentě - podle značek se pak bude provádět design

    fix window decoration hiding buttons and icons after multiple f3 or f4 presses
    fix vector image width and height with clip area mismatch
    fix popup window having 0,0 size sometimes

#### DONE
Fixed some invisible vector images + check box and radio button improvements
    added optional debug output for graphics and impl graphics
    made image content etend drawable content instead of just content
    made image content set current color before drawing (might need better workaround in the future)
    changed check box and radio button to be instances of image button
    adapted designer to deal with check box and radio button