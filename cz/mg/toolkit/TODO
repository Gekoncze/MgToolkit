#### IDEAS
    TIP: toolkit components / layouts for huge number of items - row/column virtualization "scrolling" for same sized items
    TIP: relative scroll control or scroll by components - might only be applicable to some layouts ...
    TIP: add gravity to stacks in stack layout
    TIP: add create methods instead of constructors
    TIP: raised / lowered borders
    TIP: automatic font size calculation to fit required size
    TIP: settings/parameter/attribute/style fetching for components - global vs class local vs object local vs group local settings
    TIP: možnost zadávat vzorce do spinerů
    TIP: optimalizace pro mobilní zařízení + gesta
    TIP: better bridge between drawing - something like a classic CPU to GPU converter bridge

#### MUCH LATER:
    tooltip
    zalamování textu v multiline text contentu
    designer fine tunning
        add something like a designer id property - might be better than instanceof
        přidat možnost přidávání značek ke komponentě - podle značek se pak bude provádět design - mohlo by být jen číslo + generátor

#### LATER:
    optimalizace relayoutu - u size control nastavit dva booleany
        jeden říkající, zdali okolí komponenty ovlivní její velikost
        druhý říkající, zdali vnitřek komponenty ovlivní její velikost
        poté při relayoutu doskákat vždy jen do potřebného bodu
        možná by bylo třeba upřesnit, jakého rozměru by se měl relayout týkat... a mohlo by být složitější
            might take into account that component or content width might depend on height or similar
        anebo naopak... zdali okolí/obsah určitě neovlivní její velikost!

        změřit efektivitu
            počet navštívených komponent při jednom projetí
            počet provedených relayoutů při vybrané akci

    optimalizace a polishment eventů
        neprocházet všechny větve (hlavně mouse motion eventy)
        recycling events: event objects can be reused - must be an option - user might want to have them distinguished
        add timestamp for event and eventual expiration - if gui hanged and click was processed a second later, user might not want it t be clicked at that time

#### SOON
    bring in some nice examples of everything the toolkit can do!
        most importantly - the designers!!!

    common dialogs
        color chooser dialog
        file chooser dialog (load/save)
        message dialog (none/info/warning/error)
    
#### NOW
    

#### DONE
Toplevel component + fix
    added possibility for any component to be the root component
    the component has to implement new ToplevelComponent interface
    it also has to deal with synchronization with the environment
    fixed toolkit decoration title alignment